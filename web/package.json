{
  "name": "stl-canvas-pages-spa",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync-models": "node scripts/sync-models.mjs",
    "sync-skills": "node scripts/sync-skills.mjs",
    "write-build-info": "node scripts/write-build-info.mjs",
    "predev": "npm run sync-models && npm run sync-skills && npm run write-build-info",
    "prebuild": "npm run sync-models && npm run sync-skills && npm run write-build-info"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
